rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // ESTO PERMITE QUE TU WEB ESCRIBA DATOS
      allow read, write: if true; 
    }
  }
}